import { describe, expect, it } from 'vitest'

// Placeholder test to verify the Vitest setup.
// TODO: Remove once real application tests are added.
describe('Vitest', () => {
  it('runs successfully', () => {
    expect(true).toBe(true)
  })
})
